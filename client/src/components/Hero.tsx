import { Link } from "wouter";

export default function Hero() {
  return (
    <section className="py-12 text-center">
      <h2>Hero section was removed as requested</h2>
      <p>The content has been saved to Hero-text-backup.txt</p>
    </section>
  );
}
